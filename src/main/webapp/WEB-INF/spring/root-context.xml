<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
 xmlns:util="http://www.springframework.org/schema/util"
 xmlns:task="http://www.springframework.org/schema/task"
 xmlns:context="http://www.springframework.org/schema/context"
 xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                      http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
                      http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.1.xsd
                      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- 1. 프로퍼티 설정 통합 (콤마를 사용하여 여러 설정을 단 하나의 태그로 안전하게 로드합니다) -->
	<context:property-placeholder location="classpath:config/db.properties, classpath:config/smtp.properties" />

	<!-- 데이터베이스 연결 설정 -->
	<bean class="org.apache.commons.dbcp2.BasicDataSource" id="data_source" destroy-method="close">
		<property name="driverClassName" value="${jdbc.driver}"/>
		<property name="url" value="${jdbc.url}"/>
		<property name="username" value="${jdbc.username}"/>
		<property name="password" value="${jdbc.password}"/>
	</bean>

	<!-- JavaMailSenderImpl 등록 -->
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
	    <property name="host" value="${mail.host}" />
	    <property name="port" value="${mail.port}" />
	    <property name="username" value="${mail.username}" />
	    <property name="password" value="${mail.password}" />
	    <property name="javaMailProperties">
	        <props>
	            <prop key="mail.smtp.auth">${mail.smtp.auth}</prop>
	            <prop key="mail.smtp.starttls.enable">${mail.smtp.starttls.enable}</prop>
	            <prop key="mail.smtp.starttls.required">${mail.smtp.starttls.required}</prop>
	            <prop key="mail.debug">${mail.debug}</prop>
	        </props>
	    </property>
	</bean>
	
	<!-- 메일 전송 유틸리티 빈 수동 정의 -->
	<bean id="sendMail" class="com.app.util.SendMail" />
	
	<!-- 의존성 격리 버그 방지를 위해 UserMailService 구현체를 root 영역에 수동 등록 -->
	<bean id="userMailService" class="com.app.service.user.impl.UserMailServiceImpl" />
	
	<!-- 2. MyBatis 설정 -->
	<bean class="org.mybatis.spring.SqlSessionFactoryBean" id="sqlSessionFactory">
		<property name="dataSource" ref="data_source"></property>
		<property name="configLocation" value="classpath:mybatis/mybatis-config.xml"></property>
		<property name="mapperLocations" value="classpath:mybatis/**/*_mapper.xml" />
	</bean>

	<!-- 3. DAO에서 의존성 주입받아 사용할 SqlSessionTemplate 설정 -->
	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory" />
	</bean>

	<!-- 4. 어노테이션 자동 스캔 설정 -->
	<context:component-scan base-package="com.app.service"/>
	<context:component-scan base-package="com.app.dao"/>
	<context:component-scan base-package="com.app.util"/>

</beans>
